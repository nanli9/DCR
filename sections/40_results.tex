\section{Results}
\label{sec:results}

We evaluate on the scenes of Table~\ref{tab:scenes}: DCR's own road, dinner, and
ledge figure layouts --- re-simulated with the two-way modal-constraint coupling in
place of DCR's one-way injection --- plus two modal-network stress scenes
(a box--box cube stack and a two-body full-FEM reference). Reusing DCR's layouts is
deliberate: it lets us show, on the same scenes, the back-reaction DCR cannot
produce (a distant body moved \emph{by} another body's ring through their shared
contact).

Table~\ref{tab:matrix} is the validation matrix. Each row is a metric scored against
a named reference baseline; each column is a scene. \yes\ marks a measured result
(number in the cell, subsection cited); \pend\ marks a measurement planned against
the full-FEM ground truth currently being wired; \na\ marks not-applicable. The
matrix doubles as the experiment tracker --- the honest current state is that the
contact-force ledger, the two-way network discriminator, the passivity
bound/ablation (three scenes), the restitution sweep, the slab full-FEM anchor,
and runtime on three scenes are measured; the cross-scene FEM-accuracy rows, the
momentum probe, and everything on the road scene are pending.

\paragraph*{Baselines (the reference axis).} Metrics are scored against:
\textbf{F} --- a full-FEM (unreduced) solve of the same mesh, the accuracy ground
truth \note{(consider IPC~\citep{lan2022abd}-grade contact for the reference; a
penalty/linear-implicit FEM under-resolves stiff contact)};
\textbf{D} --- DCR one-way (rigid $+$ separate IIR, no back-reaction), the base
method;
\textbf{R} --- rigid-only (modes off), the cost floor and the two-way null;
\textbf{A} --- the analytic static ledger ($\sum\lambda = mg$);
and the two internal toggles \textbf{off/on} (passivity clamp; modal network) that
isolate the two claims that are ours alone.

\begin{table*}[t]\centering\footnotesize
\setlength{\tabcolsep}{4pt}
\caption{Evaluation scenes. Rows 1--3 are DCR's own figure layouts re-simulated with
two-way coupling; rows 4--6 are our network-stress and reference scenes (the shelf
is the X-suite ablation/perf reference; the slab is the full-FEM accuracy anchor).
$n_g/n_\ell$ = global/local reduced modes per body; body counts for dinner/ledge are
the measured X5 scene sizes. $^{\dagger}$The dinner table's basis is the eigenbasis
of the same discrete FEM operator its ground truth integrates (shared operator,
\S\ref{sec:results:fem}); the scene duplicates DCR \S5.1 (2.2$\times$1.1~m table,
their Table~2 material, 5~kg pot with a parameterized drop point).
The road count is its 12 movable bodies (3 crates, 5 cones, 4 lumber) resting on
the reduced-modal road slab, counted the same movable-only way as dinner/ledge.}
\label{tab:scenes}
\begin{tabular}{llccl}
\toprule
scene & source & bodies & $n_g/n_\ell$ & what it tests \\
\midrule
Road (crate/cones)    & DCR road  & 12              & 12/16 & distant response, large slab \\
Dinner table          & DCR Fig.1/\S5.1 & 25        & 24$^{\dagger}$ & distant place-settings, drop sweep \\
Ledge (boulder)       & DCR ledge & 5               & 12/16 & strong-coupling topple \\
Cargo network (stack) & ours      & 6              & 8/1   & two-way, box--box network \\
Shelf (board+cubes)   & ours      & 6              & 10/14 & X-suite reference: ablation, perf \\
Slab + bystanders     & ours      & 1+2--5         & 16    & full-FEM accuracy anchor \\
\bottomrule
\end{tabular}
\end{table*}

\begin{table*}[t]\centering\footnotesize
\setlength{\tabcolsep}{3.5pt}
\caption{Validation matrix: metric $\times$ scene, each scored against a reference
baseline (col.\ \textbf{ref}, key above). \yes\ measured (number cited); \pend\
planned vs.\ full-FEM; \na\ not applicable. The rows no prior method can pass ---
the enforced bound (measured OFF/ON on three scenes,
\S\ref{sec:results:passivity}), the restitution consistency
(\S\ref{sec:results:restitution}), and the two-way discriminator --- are
measured; the cross-scene FEM-accuracy, momentum, and road rows are the pending
work. Runtime cells are AVBD mean ms/step (\S\ref{sec:results:perf}).}
\label{tab:matrix}
\begin{tabular}{llcccccc}
\toprule
metric & ref & Road & Dinner & Ledge & Stack & Shelf & Slab \\
\midrule
Modal displ.\ $L_2$ error          & F      & \pend & \pend & \yes\q{.38$\to$.88} & \pend & \pend & \yes\q{.56$\to$1.03} \\
Distant-response fidelity          & F,D    & \pend & \yes\q{\S\ref{sec:results:fem}} & \yes\q{$\rho$=.89} & \na   & \na & \yes\q{2.3$\times$D} \\
Contact-force ledger               & A      & \pend & \na   & \pend & \yes\q{.06--1.9\%} & \pend & \na \\
Passivity $\dEmod\!\le\!\eta\dErig$ & bound  & \pend & \yes & \yes & \yes & \yes & \pend \\
Momentum drift (lin/ang)           & $0$    & \pend & \pend & \pend & \yes\q{\S\ref{sec:results:momentum}} & \yes\q{\S\ref{sec:results:momentum}} & \pend \\
Two-way ring $|\amp|$              & off/on & \pend & \pend & \pend & \yes\q{1.1e{-}5/0} & \pend & \na \\
Restitution ratio $\le1\ \forall\varepsilon_r$ & D & \na & \yes\q{.05 vs 2.6} & \na & \na & \na & \na \\
Runtime (ms/step)                  & R,D    & \pend & \yes\q{33.7} & \yes\q{15.8} & \pend & \yes\q{17.1} & \na \\
Passivity ablation (blow-up)       & off/on & \na   & \yes\q{5.1e3$\times$} & \yes\q{1.2e5$\times$} & \na & \yes\q{1.1e4$\times$} & \na \\
\bottomrule
\end{tabular}
\end{table*}

\subsection{Contact forces balance a cube stack}
\label{sec:results:ledger}
\note{Numbers below are read straight off the solver's contact multipliers; see
\texttt{docs/sheldon\_report/} and its reproduce script --- analysis only, no solver
code changed.} A modal slab carries a plain resting control cube and a 3-high zig-zag
stack (base/mid/upper); each cube carries its own reduced modes. Reading the
multipliers (Table~\ref{tab:ledger}), each joint carries exactly the weight above it,
with no per-joint tuning --- the shared multiplier balances the tower on its own.

\begin{table}[t]\centering
\caption{Static contact ledger (settled), $m g = 5.886$~N.}
\label{tab:ledger}
\begin{tabular}{lccc}
\toprule
joint & measured & expected & error \\
\midrule
resting cube $\to$ slab                & 5.889~N & $mg$  & 0.06\% \\
base cube $\to$ slab                   & 17.99~N & $3mg$ & 1.9\%  \\
base $\leftrightarrow$ mid (box--box)  & 11.93~N & $2mg$ & 1.4\%  \\
mid $\leftrightarrow$ upper (box--box) & 5.82~N  & $mg$  & 1.1\%  \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure*}[t]\centering
  \includegraphics[width=\linewidth]{fig_contact_forces.pdf}
  \Description{Two panels. Left: the top cube's steady modal ring amplitude on a
  log scale for native fem-rigid and abd cargo versus rigid cargo and network-off,
  the latter two identically zero. Right: each joint's measured normal load beside
  the analytic stack-above times mg, matching within 1.9 percent.}
  \caption{(a) The two-way discriminator: the top cube --- two box--box hops from
  the slab --- carries a steady ring only when the modal network is on (native
  fem-rigid / abd), and $|\amp|\!\equiv\!0$ for rigid cargo or network-off. (b) The
  static contact-force ledger: each joint's measured normal load matches the
  analytic $(\Sigma\,\text{above})\,mg$ to $<1.9\%$ --- the shared multiplier
  balances the tower with no per-joint tuning.}
  \label{fig:forces}
\end{figure*}

\subsection{Two-way through the network}
\label{sec:results:network}
The \texttt{upper} cube touches only \texttt{mid}; with the modal contact network on
it rings ($|\amp|$ peak $\approx 1.1\times10^{-5}$), off it is identically zero. The
vibration reaching it arrives only through the shared contacts. The modal compliance
also \emph{smooths} the box--box force (joint-force ripple $\pm2.72$~N off
$\to \pm1.48$~N on, mean unchanged at $mg$).

\subsection{Full-FEM ground truth}
\label{sec:results:fem}
On the slab reference (one corner-fixed $10\times6\times2$ FEM slab carrying an
impactor and a bystander; both arms integrate the \emph{same} operator, the
native arm using its true eigenmodes), the reduced-vs-full-FEM mid-deflection
ratio converges $0.56 \to 0.75 \to 0.90 \to 1.03$ as the rigid step refines
$h = 1/120 \to 1/960$: the coupling magnitude is \emph{correct}, and the gap at
the paper step is the temporal resolution of a sub-millisecond impulse
delivered over one $8.3$~ms step, not a coupling error. The ring frequency
matches at $81.0$ (GT) vs $80.7$~Hz (native), $0.4\%$ --- read at $h=1/480$,
since at the paper step the $81$~Hz ring aliases against the $120$~Hz sampling
(the modes carry $\omega_j$ exactly at every $h$ via $K_q$). Truncation is not
the bottleneck: the distant deflection field is captured by the first
${\sim}2$ bending modes (field error $35.8\%$ at $k{=}1$, ${\approx}27$--$30\%$
for $k\ge2$ --- the plateau is the timestep floor above, not mode count). Two
honesty notes calibrate everything else we report. First, the trustworthy GT
observable is the \emph{surface deflection field}: the bystander's launch KE
does \emph{not} converge under GT refinement ($158\to39\to69$~mJ as
$h_{\mathrm{GT}}$ halves --- a ${\sim}2$~kHz penalty-contact oscillator the GT
resolves poorly) while the field is stable to $1\%$; launch quantities are
therefore secondary observables for both arms. Second, the validated falloff
is the slab's low-mode \emph{standing-wave} profile --- non-monotonic with
distance and reproduced by the native arm with no $C\,r^{-\beta}$ term at
every $h$ --- not traveling-wave decay; the no-attenuation-term advantage is
claimed for this modal regime only. At the paper step the native arm costs
$13.6\times$ less wall-clock than the full-FEM GT on this scene.
A tet-refinement ladder on the ground-truth meshes (R0--R4, up to
67k nodes per support) shows the linear-tet operators themselves are still
$3$--$10\%$/rung from the continuum (CST bending lock releases slowly), so all
ground-truth frequencies and sags are reported as properties of the
\emph{shared discrete operator}, never as physical plate values --- both arms
use the same operator, so discretization bias cancels from every ratio.

\paragraph*{Ledge --- a second ground-truth scene.} The strong-coupling ledge
(a $16$~kg boulder dropped off-centre onto a stiff $1.2{\times}0.8{\times}0.08$~m
stone slab that also carries a pedestal) gives the cross-scene accuracy check.
Built on the same shared operator (native modes $=\mathrm{eigsh}(K,M)$ to
$6\times10^{-8}$; GT self-trust $0.18\%$ under an $h_{\mathrm{GT}}$ halving;
Fig.~\ref{fig:ledge}), the
reduced-vs-full-FEM peak-deflection ratio rises monotonically $0.38\to0.54\to
0.79\to0.88$ across $h=1/120\to1/960$ and plateaus near $0.9$ at finer steps
($0.94$/$0.87$ at $1/1920$/$1/3840$): the residual ${\sim}10\%$ is $k{=}24$ mode
truncation on the thick, higher-frequency slab --- the ring frequency already
matches at $78.0$ (native) vs $78.3$~Hz (GT), $0.4\%$ --- not a coupling error,
and it is the same pre-topple, small-displacement regime the Limitations state.
The distant response is again the modal \emph{standing wave}: the peak deflection
sits at the pedestal (the antinode), \emph{not} under the impact, and the native
falloff along the slab tracks the GT with Spearman $\rho=0.89$ --- no fitted
attenuation term, on a second scene.

\begin{figure*}[t]\centering
  \includegraphics[width=\linewidth]{fig_ledge_gt.pdf}
  \Description{Two panels for the ledge ground-truth comparison. Left: the
  native-over-ground-truth peak deflection ratio rising from 0.38 to 0.88 as the
  rigid step refines from 1/120 to 1/960, toward a dashed line at 1.0. Right:
  peak vertical deflection versus distance from the boulder impact for the native
  arm and the full-FEM ground truth, both peaking at the pedestal rather than the
  impact.}
  \caption{Ledge, second ground-truth scene. (a) The native/GT peak-deflection
  ratio converges toward the GT as the rigid step refines (ring frequency already
  matched to $0.4\%$); the residual is $k{=}24$ mode truncation on the thick slab.
  (b) Peak $|u_y|$ along the slab: native (blue) tracks the full-FEM GT (black
  dashed), and both peak at the \emph{pedestal antinode}, not under the impact ---
  the modal standing wave, reproduced with no fitted attenuation
  ($\rho=0.89$).}
  \label{fig:ledge}
\end{figure*}

On the dinner scene (DCR \S5.1 duplicate: their Table~2 material, 5~kg pot,
dishes rigid exactly as in their \S4.5), the pot is dropped at three points
along the 2.2~m table against an all-FEM ground truth in which \emph{every}
body is elastic (implicit Newmark at $h=5\times10^{-5}$~s, ${\sim}18$~min per
1.2~s drop vs.\ ${\sim}34$~ms/step for ours, \S\ref{sec:results:perf}). The two arms agree on the operator ---
the ground truth's measured ring ($9.2$--$9.9$~Hz) matches the native basis
$f_1 = 9.8$~Hz --- and the native response falls off with distance from the
drop point with \emph{no fitted attenuation model} (DCR fits its Eq.~14
$C,\beta$ by hand or to data; Fig.~\ref{fig:dinner}). The native arm
under-predicts absolute dish-launch amplitude --- ${\sim}7\times$ at the
loudest near-field dishes (Fig.~\ref{fig:dinner}), median launch ratio
$0.016$ over all 72 scored bodies at the paper step --- and an $h$-ladder
pins the mechanism: the ratio recovers \emph{monotonically},
$0.016 \to 0.052 \to 0.136 \to 0.257$ as $h = 1/120 \to 1/960$
(${\sim}2$--$3\times$ per halving; insensitive to mode count $24\to48$ and
under-relaxation $0.7\to1.0$; per-step cost unchanged at ${\sim}35$~ms).
Launch is the harshest observable of the impact transient (ballistic height
$\propto v^2$ of a sub-millisecond surface-velocity peak that the rigid step
low-passes), and it is also the observable the GT itself resolves poorly
(above), so the residual gap at $1/960$ \emph{bounds the true deficit from
above} rather than measuring it --- the GT-convergent deflection field is
the accuracy metric, and it reaches $1.03$. DCR's IIR-plus-kick architecture
(their Eqs.~10--12) makes the opposite trade: it sub-steps the filter and
re-injects the \emph{maximum} displacement as a velocity change,
deliberately re-amplifying that transient --- and thereby exceeding the
passive bound as $\varepsilon_r\!\to\!1$
(\S\ref{sec:results:restitution}). Recovering the transient passively costs
timestep ($8\times$ the steps $\Rightarrow$ ${\sim}34$~s per simulated
second at $1/960$, still ${\sim}22\times$ under the all-FEM GT); recovering
it for free costs the guarantee.

\begin{figure*}[t]\centering
  \includegraphics[width=\linewidth]{fig_dinner_response.pdf}
  \Description{Three log-scale panels of peak upward jump versus distance
  from the drop point, comparing the native modal solver, the all-cargo
  variant, and the all-FEM ground truth.}
  \caption{Dinner drop-position sweep: per-body peak upward jump vs.\ distance
  from the pot's drop point (log scale), three drop points. The native
  rigid-dish arm (DCR's own \S4.5 configuration) reproduces the ground truth's
  distance falloff at ${\sim}7\times$ lower amplitude (band-limiting, see
  text); the all-cargo variant (every dish deformable, our generalization)
  responds less again because soft bodies absorb impact compliantly. Toppled
  bodies ($>0.3$~m) are excluded per the ground-truth contact limitations.}
  \label{fig:dinner}
\end{figure*}

\subsection{Head-to-head with DCR on one operator}
\label{sec:results:dcr}
Figure~\ref{fig:x2falloff} runs the slab reference under three arms --- rigid-only
(\textbf{R}), paper-DCR (\textbf{D}: the Eq.~10 forced IIR plus the post-solve
$\Delta v = d_{\max}/h$ kick), and the native constraint --- and measures each
bystander's peak KE against its distance from an off-centre impact. Rigid-only
produces no distant response (identically zero beyond the nearest object: the
machinery is genuinely off). Paper-DCR is strongest near the impact
($13.5$~mJ at $0.14$~m) but collapses to $0.9$~mJ by $0.42$~m. The native arm
starts softer ($9.8$~mJ) and falls off gently, delivering $2.0$~mJ at
$0.56$~m --- $2.3\times$ the paper-DCR response at the farthest object; the two
curves \emph{cross}. The native falloff is the slab's standing-wave modal
profile (\S\ref{sec:results:fem}) with no $C\,r^{-\beta}$ term and no per-scene
response parameters (paper-DCR hand-sets $C,\beta,r_0$ and its Rayleigh pair);
its steeper decay is the impulse-localized forced response. Honesty: this is a
method-level comparison on one shared operator, each arm at its natural
timestep --- the comparand is the falloff \emph{shape}, which is
$h$-independent (\S\ref{sec:results:fem}), not absolute magnitude.

\begin{figure}[t]\centering
  \includegraphics[width=\linewidth]{fig_x2_falloff.pdf}
  \Description{Peak bystander kinetic energy versus distance from impact for
  rigid-only, paper-DCR, and native arms on the shared slab operator.}
  \caption{Response vs.\ distance, three arms on one slab operator. Paper-DCR
  is strong near-field and collapses with distance; the native standing-wave
  response reaches the far bystander at $2.3\times$ the DCR level; rigid-only
  is zero. \note{draft figure (X2 output) --- restyled in the figure pass.}}
  \label{fig:x2falloff}
\end{figure}

\subsection{The passivity bound bites when it must}
\label{sec:results:passivity}
Two experiments separate ``naturally passive'' from ``needs the governor'':
an adversarial budget$\times$relaxation sweep, and the budgets solvers
actually ship.

\paragraph*{Adversarial sweep (Fig.~\ref{fig:x1robust}).} XPBD (the injecting
formulation, \S\ref{sec:method:solve}) on three scenes (shelf, ledge, dinner)
$\times$ relax $\{0.7, 1.0\}$ $\times$ budget $\{4{\times}1, 8{\times}2,
16{\times}4, 32{\times}8\}$, clamp OFF vs ON; ``passivity'' is the peak
support-ring energy over the impactor KE. OFF: \textbf{12 of 24 cells inject},
with per-scene worst cases $1.1\times10^{4}$ (shelf), $1.2\times10^{5}$
(ledge), $5.1\times10^{3}$ (dinner). ON: \textbf{all 24 cells satisfy the
ledger invariant}, and every injecting cell lands at $\le 1.22$. At the paper
configuration ($16{\times}4$, relax $0.7$) the clamp fires \textbf{zero times
on all three scenes} and the trajectories are unchanged --- the governor is
inert exactly where the solver is already passive. On the shelf drop the
enforcement is machine-tight: the un-clamped $8{\times}2$ ring peaks at
$53.7\times$ the impactor KE, the clamped run sits at $1.01$, and the maximum
net ledger excess is ${\sim}3\times10^{-17}$~J.

\paragraph*{Production budgets.} At the substep-heavy budgets XPBD ships
($1{\times}16$, $2{\times}4$, $1{\times}8$) the un-governed modal block
injects $5.5\times10^{4}$ to $9.2\times10^{6}$~J into a scene holding
${\sim}1$~J; the clamp bounds every case to $36$--$42$~J. AVBD at
$4{\times}4$ / $2{\times}4$ is \emph{bit-identical} with the clamp off or on
($0.91$/$0.79$~J, zero activations): on the descent-class solver passivity is
free and the ledger is a witness, not an actor --- the dichotomy of
\S\ref{sec:method:solve} measured.

\paragraph*{Enforcement as shipped.} The budget each substep is the
\emph{irreversible} contact loss, $\mathrm{grav\ work} - \Delta \mathrm{KE}$
(zero in ballistic flight, positive only when contact dissipates); losses
accumulate in a reservoir so the guarantee is cumulative,
$\sum \dEmod \le \eta \sum \dErig$, and stays inert while slack remains; a
substep that would overshoot is projected back by scaling the modal state
$(\amp,\dot\amp)$ by a factor $\gamma$ with
$\gamma^2 = (E_{\mathrm{old}}+\mathrm{budget})/E_{\mathrm{new}}$, landing
exactly on the ceiling. \note{Method pass: state
in \S\ref{sec:method:passivity} that the shipped clamp is this cumulative,
full-state generalization of the per-step velocity-kick bound of
Eq.~\ref{eq:alphastar} --- same budget, projection of $(\amp,\dot\amp)$ rather
than a scale on the kick alone.}

\paragraph*{The price, honestly.} The ledger costs $+0.5$--$4.8$~ms/step at
the paper configuration (single-run; re-measured with repetitions in the
performance pass) and up to $+37$--$64\%$ on XPBD at extreme substep counts
--- host read-backs in the energy accounting, an implementation cost, not
fundamental. The physical price is one-sided: on AVBD the clamp never fires
(zero cost); on XPBD it prevents the blow-up but \emph{over-damps} the ring
--- cap-ON $|\amp|\approx1.2\times10^{-5}$ vs $6\times10^{-4}$ for converged
XPBD and $8\times10^{-5}$ for the FEM-validated AVBD reference --- because
the injected state is $\dot\amp$-heavy and the $\gamma$ projection shrinks
$\amp$ with it. The cap makes the livelier host \emph{stable}, not
\emph{accurate}; AVBD is the accurate default on which the guarantee is free.

\paragraph*{Friction.} The bound is stated on \emph{energy}, not impulses, so
it is agnostic to \emph{how} contact removes rigid energy: the ledger credits
$\eta$ times the rigid mechanical-energy loss ($\Delta\mathrm{KE}$ plus gravity
work), which already subsumes tangential (Coulomb) dissipation --- there is no
separate normal-vs-tangential accounting to get wrong. Sweeping the box--box
stack at $\mu\in\{0,0.2,0.5,1.0\}$ confirms it: the net invariant holds in
$4/4$ cases, the injection ratio is flat at $0.26$--$0.28$ of the budget, and
there are \emph{zero} clamp activations at every $\mu$ (AVBD monitor-only).
Friction enlarges the loss budget rather than opening an unbounded tangential
modal channel, so the guarantee needs no tangential term.
\note{E6; \texttt{benchmarks/paper\_eval/x1\_passivity/friction\_bound.csv}.}

\begin{figure*}[t]\centering
  \includegraphics[width=\linewidth]{fig_x1_robust.pdf}
  \Description{Grid of passivity ratios across scenes, budgets, and
  relaxation values, clamp off versus on.}
  \caption{Adversarial robustness: XPBD across scene $\times$ budget $\times$
  relaxation, clamp OFF (left) vs ON (right). OFF injects in 12/24 cells (up
  to $1.2\times10^{5}\times$); ON passes the ledger invariant in 24/24 and is
  bit-inert in the already-safe region. \note{draft figure (X1 output) ---
  restyled in the figure pass.}}
  \label{fig:x1robust}
\end{figure*}

\subsection{Momentum: exact rows, truncated sweeps}
\label{sec:results:momentum}
A read-only probe isolates the momentum story on the registered box--box pair
in free flight (gravity zeroed, pair teleported clear of every other body).
Free flight conserves the pair's $P$ and $L$ \emph{exactly} (drift $0.0$ over
the window). Through a $1.5$~m/s impact, however, the \emph{host} solver's
truncated block sweep applies the row asymmetrically and pair momentum is
created: $+47\%$ at the shipped $12{\times}4$ budget, $+12\%$ at
$32{\times}4$, $+1.8\%$ at $64{\times}4$, $+0.2\%$ at $12{\times}16$ ---
vanishing with budget, fastest with substeps. The error is \emph{identical}
with the modal network on or off and with fully rigid cubes: the modal
coupling adds \textbf{no momentum error beyond the host's own truncation
error}, which scopes the third-law claim of \S\ref{sec:method:constraint} ---
exact per row, budget-limited per sweep, the momentum-side twin of the energy
story that motivates the bound. The clamp itself is momentum-silent: clamped
and unclamped runs (XPBD $8{\times}2$ shelf, 183 activations) are
bit-identical on every frame before the first activation, and diverge
afterwards only through the support geometry subsequent contacts see --- the
$\gamma$ projection touches modal state only, never rigid momenta.
\note{E4 probe; CSVs in \texttt{benchmark/runs/momentum/}; per-scene drift
cells and a figure land with the performance pass.}

\subsection{Restitution: bounded where the forced IIR double-counts}
\label{sec:results:restitution}
DCR's own \S5.4 flags the flaw: as $\varepsilon_r\to1$ a contact dissipates
almost nothing, so a response driven by the contact \emph{impulse} keeps
injecting vibration energy that no longer exists to give. We sweep the pot's
$\varepsilon_r$ on the dinner drop and measure, per impact, injected vibration
energy over rigid contact loss (Fig.~\ref{fig:x7}). Paper-DCR: the loss
collapses $7.30\to0.20$~J while the forced-IIR injection \emph{grows}
$0.15\to0.52$~J; the ratio crosses $1$ near $\varepsilon_r\approx0.8$ and
plateaus at $2.63$--$2.64\times$ --- energy created. Native: $0.053$ at every
$\varepsilon_r$ --- the clamp funds modal gain from measured rigid loss, so
restitution cannot break the bound \emph{by construction}. Two honesty notes:
the accounting is windowed per impact, so the exact crossing point is
scene/window-dependent (the trend --- loss vanishes, injection persists --- is
the robust signal); and the native contact is $e{=}0$ (no restitution term in
the native solver yet), so its curve is the bounded reference, not a
matched-restitution comparison.

\begin{figure}[t]\centering
  \includegraphics[width=\linewidth]{fig_x7_restitution.pdf}
  \Description{Injected-over-lost energy ratio versus coefficient of
  restitution for paper-DCR and the native bounded arm.}
  \caption{Restitution sweep: injected/lost energy per impact. Paper-DCR
  crosses $1$ near $\varepsilon_r\approx0.8$ and reaches $2.6\times$
  (double-counting); the native arm stays at $0.05\le1$ for all
  $\varepsilon_r$. \note{draft figure (X7 output) --- restyled in the figure
  pass.}}
  \label{fig:x7}
\end{figure}

\subsection{Runtime at the paper configuration}
\label{sec:results:perf}
Table~\ref{tab:perf} gives CPU-host timings at the paper configuration
(single machine, 100 frames after warm-up, symplectic modal path --- the
configuration every result above validates). AVBD is $1.6$--$3.1\times$
faster than XPBD here (XPBD pays a per-mode Gauss--Seidel sweep;
dinner at 17 bodies / 24 modes is its worst case). The honest split behind
the title. The fastest validated \emph{CPU-host} configuration (ledge/AVBD,
$15.8$~ms, ${\sim}63$~steps/s) is interactive but not $120$~Hz real-time. The
same solver also runs device-resident: its co-solved (non-symplectic) modal
step, with a warp-cooperative modal solve and full-substep CUDA-graph capture,
measured here on an RTX~4090 (Table~\ref{tab:perfdev}, Fig.~\ref{fig:device}). At the paper budget it
runs at $5.6$/$6.8$/$8.9$/$9.2$~ms/step (shelf/ledge/truck/dinner): shelf and
ledge already clear $120$~Hz ($1.5\times$/$1.2\times$) and dinner and truck sit
at $109$--$113$~steps/s, within $10\%$ of it. The co-solved cost is linear in
the iteration budget, so $16{\times}2$ puts every scene comfortably real-time
($3.3$/$4.7$~ms on ledge/dinner) and $8{\times}1$ reaches $0.9$--$1.3$~ms
($795$--$1092$~steps/s). Because the resident $q$-solve is bit-identical to the
CPU reference, the two paths differ only in the modal integrator (host
symplectic vs.\ co-solved), not the constraint. Critically, a read-only
$\S15$ ledger evaluated over the full trajectory of all four scenes across five
budgets ($8{\times}1$ to $32{\times}4$) finds the device path \emph{passive in
$20/20$ cells with no active clamp}: peak modal energy never reaches its start
value plus $\eta$ times the cumulative rigid loss (net excess $<0$ in every
cell), including the $8{\times}1$ real-time budget where a stationarity-class
host would inject. The passivity guarantee therefore \emph{does} cover the
device path --- it is an AVBD descent-class property, the same one that makes
the clamp of \S\ref{sec:results:passivity} monitor-only on AVBD, not a separate
mechanism. We keep interactive rates as the headline only because the
deflection-field accuracy of \S\ref{sec:results:fem} is validated on the
symplectic modal step; reproducing it on the co-solved step is the one
remaining device-path item. For context, the full-FEM ground truth costs
$13.6\times$ the native arm on the slab scene (\S\ref{sec:results:fem}).
\note{Device numbers: RTX~4090, warp 1.15, 200 frames after warm-up,
\texttt{benchmarks/paper\_eval/x5\_perf/run\_perf\_device.py} +
\texttt{probe\_device\_passivity.py}; CPU table is single-run, re-measured with
reps + R/D baselines in the perf pass.}

\begin{table}[t]\centering\footnotesize
\setlength{\tabcolsep}{4pt}
\caption{ms/step at the paper configuration (CPU host, single run). ``clamp''
is the passivity-ledger overhead included in the mean.}
\label{tab:perf}
\begin{tabular}{llrrrr}
\toprule
scene & solver & mean & worst & clamp & steps/s \\
\midrule
shelf  & XPBD & 26.9 & 43.0 & +2.4 & 37.2 \\
shelf  & AVBD & 17.1 & 20.8 & +4.8 & 58.6 \\
ledge  & XPBD & 42.0 & 63.6 & +3.0 & 23.8 \\
ledge  & AVBD & 15.8 & 17.3 & +0.5 & 63.4 \\
dinner & XPBD & 102.9 & 137.6 & +2.6 & 9.7 \\
dinner & AVBD & 33.7 & 41.4 & +1.8 & 29.7 \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[t]\centering\footnotesize
\setlength{\tabcolsep}{5pt}
\caption{Device-resident co-solved path (RTX~4090, warp modal solve +
full-substep CUDA-graph capture, 200 frames). ms/step for the two-way modal
support DOF; the resident $q$-solve is bit-identical to the CPU reference.
Real-time is ${\le}8.33$~ms/step ($120$~Hz, shaded). The $16{\times}2$ and
$8{\times}1$ budget columns are measured on ledge/dinner; a read-only $\S15$
ledger confirms passivity in all $20$ scene$\times$budget cells with no active
clamp.}
\label{tab:perfdev}
\begin{tabular}{lrrrrr}
\toprule
scene & modes & $16{\times}4$ & $16{\times}2$ & $8{\times}1$ & steps/s \\
\midrule
shelf  & 16 & \textbf{5.6} & --  & --  & 177 \\
ledge  & 16 & \textbf{6.8} & \textbf{3.3} & \textbf{0.9} & 147 \\
truck  & 21 & 8.9 & --  & --  & 113 \\
dinner & 24 & 9.2 & \textbf{4.7} & \textbf{1.3} & 109 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure*}[t]\centering
  \includegraphics[width=\linewidth]{fig_device_runtime.pdf}
  \Description{Two panels for the device path. Left: milliseconds per step versus
  iteration budget for dinner and ledge, with a 120 Hz real-time line and a
  shaded real-time region below it; both scenes enter the region below a
  16-by-4 budget. Right: realized modal energy gain versus the eta-times-rigid-
  loss budget for all twenty scene-budget cells, every point below the passive
  diagonal.}
  \caption{Device-resident co-solved path (RTX~4090). (a) ms/step is linear in
  the iteration budget; both swept scenes cross into the $120$~Hz real-time
  region (shaded) at $16{\times}2$ and below. (b) The $\S15$ ledger over all
  $20$ scene$\times$budget cells: realized modal gain stays under the
  $\eta\,\Delta E_{\rm rigid}$ budget (the passive diagonal) at every budget with
  \emph{no active clamp} --- the real-time path is passive by construction.}
  \label{fig:device}
\end{figure*}
